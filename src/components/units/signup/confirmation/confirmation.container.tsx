import React from 'react';
import ConfirmationUI from './confirmation.presenter';

const Confirmation = ({ navigation }: any) => {
  return <ConfirmationUI navigation={navigation} />;
};
export default Confirmation;
